# UniDeploy API — Python Dependencies

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database (InsForge uses httpx REST client — no ORM SDK needed)
sqlalchemy[asyncio]==2.0.23
psycopg2-binary>=2.9.0
asyncpg>=0.30.0
alembic==1.13.1

# Rate limiting
slowapi>=0.1.9

# HTTP + multipart
httpx>=0.28.1,<1.0.0
python-multipart==0.0.9

# Google Gemini ADK + direct Gemini client
google-adk>=1.0.0
google-genai>=1.0.0

# E2B sandbox — code runs inside VM, never on Cloud Run disk
e2b>=1.0.0

# Composio — GitHub PR creation for FixAgent
composio-core>=0.7.0

# Observability (import commented out in main.py until metrics router is wired)
prometheus-client>=0.17.0

# Testing
pytest>=8.0.0
