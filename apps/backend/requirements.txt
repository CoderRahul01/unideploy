# UniDeploy API — Python Dependencies

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Rate limiting
slowapi>=0.1.9

# HTTP + multipart
httpx>=0.28.1,<1.0.0
python-multipart==0.0.9

# Google Gemini ADK + direct Gemini client + Vertex AI (Agent Engine caller)
google-adk>=1.0.0
google-genai>=1.0.0
google-cloud-aiplatform>=1.60.0

# E2B sandbox — code runs inside VM, never on Cloud Run disk
e2b>=1.0.0

# Composio — GitHub PR creation for FixAgent
composio-core>=0.7.0

# Observability (import commented out in main.py until metrics router is wired)
prometheus-client>=0.17.0

# JWT verification (Clerk backend token validation)
PyJWT[crypto]>=2.9.0

# Testing
pytest>=8.0.0
