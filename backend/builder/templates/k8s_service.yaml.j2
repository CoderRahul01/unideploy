apiVersion: v1
kind: Service
metadata:
  name: {{project_name}}
spec:
  selector:
    app: {{project_name}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{port or 80}}
  type: ClusterIP
